#include "Button.h"

// __Pending
/*

// This is an extension of BUtton into a drop down menu
// We do not have side-pop style buttons in our application, and not intent to provide one
class DropDownButton : public Button
{
public:
	DropDownButton(DropDownPopupMenu* menu);
	~DropDownButton();


private:
	DropDownPopupMenu* dropDownMenu;
	Renderable2DImageQuad* secondaryIcon = NULL;	// Useful for landmark drop down menu on the Canvas
	Renderable2DImageRect* background = NULL;	// Make a modern frame for Drop Down button
};
*/